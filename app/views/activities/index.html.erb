<div>
<h1>Activities available</h1>
<ul>
  <% @activities.each do |activity| %>
    <h2><%= link_to activity.name, activity %></h2>
    <p><%= activity.description %></p>
  <% end %>
</ul>
<%= link_to 'Add new activity', new_activity_path, class: "btn btn-plus" %>
</div>
<%# render partial: "/shared/activity_card", collection: @activities, as: :activity %>
